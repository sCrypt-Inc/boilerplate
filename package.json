{
  "name": "scrypt-ts-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "clean-test-out": "rimraf scrypt.index.json && rimraf out && rimraf scrypts",
    "pretest": "npm run clean-test-out && npm run build",
    "test": "mocha 'dist/tests/*.test.js' --timeout 1200000",
    "pretestnet": "npm run pretest",
    "testnet": "mocha 'dist/testnet/*.js' --timeout 1200000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "=1.1.3",
    "scrypt-ts": "=0.1.3-alpha.5"
  },
  "devDependencies": {
    "@types/chai": "^4.3.4",
    "@types/mocha": "^10.0.0",
    "@types/node": "^18.11.9",
    "chai": "^4.3.7",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "mocha": "^10.1.0",
    "rimraf": "^3.0.2",
    "ts-patch": "^2.0.2",
    "typescript": "=4.8.4"
  }
}
