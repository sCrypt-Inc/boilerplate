{
  "name": "scrypt-ts-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "ts-patch install -s",
    "build": "tsc",
    "clean-test-out": "rimraf scrypt.index.json && rimraf out && rimraf scrypts",
    "pretest": "npm run clean-test-out && cross-env TS_NODE_PROJECT=tsconfig.json NODE_ENV=test tsc",
    "test": "mocha 'dist/**/*.test.js' --timeout 1200000"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "=1.1.3",
    "scrypt-ts": "=0.1.1-alpha",
    "scryptlib": "=1.1.0-beta"
  },
  "devDependencies": {
    "@types/chai": "^4.3.4",
    "@types/mocha": "^10.0.0",
    "@types/node": "^18.11.9",
    "chai": "^4.3.7",
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "mocha": "^10.1.0",
    "rimraf": "^3.0.2",
    "ts-patch": "^2.0.2",
    "typescript": "=4.8.4"
  }
}
